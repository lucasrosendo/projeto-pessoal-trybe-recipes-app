// Importação do createContext do react
import { createContext } from 'react';

// Criando o Context e vinculando o mesmo ao RecipeContext
const RecipeContext = createContext();

export default RecipeContext;
